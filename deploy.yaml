---
  - hosts: all
    gather_facts: true

    tasks:
    - name: Secure Server
      import_role:
        name: geerlingguy.security
      vars:
        security_ssh_port: 22
        security_ssh_password_authentication: "yes"
      become: true
