---
  - hosts: all
    gather_facts: true
    remote_user: '{{ service_user }}'

    tasks:

    - name: Import geerlingguy.security role
      import_role:
        name: geerlingguy.security
      become: true
